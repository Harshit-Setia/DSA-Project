import javax.swing.*;

public class QuizPage {
    public QuizPage(String prn) {
        JFrame frame = new JFrame("Take Quiz");
        frame.setBounds(100, 100, 400, 300);
        frame.setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE);
        frame.setVisible(true);
    }
}